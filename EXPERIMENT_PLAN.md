# 통합 실험 계획서

> 작성일: **2026-08-17 KST**  
> 검토·개정일: **2026-08-20 KST — V3 실제 제출 결과 연결**  
> 리더보드 제출 마감: **2026-09-01 10:00 KST — 남은 기간 15일**  
> 근거 문서: [`LOCAL_ENVIRONMENT.md`](LOCAL_ENVIRONMENT.md) · [`COMPETITION.md`](COMPETITION.md) · [EDA 제1·2부](eda/EDA_REPORT.md) · [베이스라인](experiments/BASELINE_REPORT.md) · [Calibration·Ensemble](experiments/CALIBRATION_ENSEMBLE_REPORT.md) · [야구 도메인 로드맵](research/RELATED_WORK_AND_EXPERIMENT_ROADMAP.md) · [유사 도메인 방법론](research/ANALOGOUS_DATA_METHODS_AND_EXPERIMENTS.md)

---

## 0. 한 장 요약

| 항목 | 현재 |
| --- | --- |
| 로컬 CV 기준 후보 | S4 (S2 + E14 + `r_recent3`), Linear 90% + HGB 10% |
| 최종 로컬 앙상블 | S8 M3: S4 70% + S5 5% + S6 10% + S7 15% |
| 당시 LB 챔피언 (v1) | S8 최종 M3, `689.3999289563` |
| 현재 LB 챔피언 | V3 sparse M3, **`1090.9100565103`** (제출 ID `57386`) |
| S4 rolling 3-fold 평균 Brier | `0.24756026` (3/3 fold 개선) |
| S8 M3 rolling 3-fold 평균 Brier | `0.24755460` (S4 대비 평균 `-0.000005657`, 2/3 fold 개선) |
| 현재 로컬 환경 | Windows 11, Ryzen 5 5600, RAM 31.9GB, Python 3.11.9 `.venv` |
| **리더보드 제출 이력** | **S1 `549.5119345223`, S2 `527.6161010151`, S3~S6·S8 제출 완료; S7 미제출** |
| 수료 기준 | Public Score `549.51` 이상 |

**전략 세 줄.**

1. **제출 파이프라인과 기준 제출을 먼저 고정했다.** S1·S2의 서버 실행 결과를 운영 앵커로 보존하고, 이후 후보는 ZIP 게이트 통과 뒤 업로드하지 않고 동결한다.
2. **`E14`/`E15`를 누수 없는 rolling으로 검증했다.** E14 all-history의 S3와 E15 `r_recent3`의 S4가 각각 3/3 fold 개선했고, S4를 후속 실험의 기준 후보로 삼는다.
3. **후속 실험과 최종 앙상블을 독립 비교했다.** 이 문서 범위에서는 S8을 동결했고,
   후속 V3에서 sparse M3가 실제 `1090.9100565103`으로 새 챔피언이 됐다.

**핵심 근거.** E14·E15의 최종 rolling 결과와 후속 실험·앙상블 비교는 [`experiments/EXPERIMENT_REGISTRY.md`](experiments/EXPERIMENT_REGISTRY.md)와 각 `experiments/results/archive/*`에 저장했다. 최종 M3는 S4 대비 평균 Brier `-0.000005657`이지만 2022 fold에서는 `+0.000021785` 악화가 있어, 단일 승리로 과장하지 않고 S4·S5·S6·S7·S8을 모두 선택 가능한 후보로 유지한다.

---

## 1. 전략의 전제: Private = Public이 바꾸는 것

[`COMPETITION.md` §7.2](COMPETITION.md)에 따르면:

- Public Score = **전체 테스트 데이터 100%**
- Private Score = 대회 종료 시점의 Public Score
- 별도 홀드아웃 분할이 없다

이것이 통상적인 대회 전략을 뒤집는다.

| 통상적인 대회 | 이 대회 |
| --- | --- |
| Public LB 과적합이 Private에서 무너진다(shake-up) | 현재 공지대로 동일 점수를 확정한다면 **데이터 분할에 의한 shake-up은 없음** |
| 로컬 CV를 주 검증으로 사용 | **시간 순 CV로 후보 선택**, LB로 실제 2025 정렬 여부 확인 |
| 제출은 검증 수단 | 제출은 **최종 확인 수단**이며, 가설이 있는 후보만 올림 |
| Public/Private 후보를 별도로 관리 | 동일 평가셋 전제에서는 하나의 후보군을 관리하되 코드 검증 위험도 함께 고려 |

동일 파일의 점수는 결정적이지만, 245,789행이 서로 완전히 독립인 것은 아니고 투수·경기 군집이 존재한다. 또한 여러 제출의 LB 결과에 맞춰 계속 설계를 바꾸면 적응형 selection bias와 평가 데이터 역추정 위험이 생긴다. 따라서 **허용된 75회를 소진하는 것이 목표가 아니라, 로컬 게이트를 통과한 과학적으로 구분되는 후보만 제출하는 것**을 원칙으로 한다.

### 1.1 확인 완료 — 최종 반영값은 최고 점수다

2026-08-17 19:55 KST에 [공식 DAKER FAQ](https://dacon.io/competitions/official/236743/talkboard/417082?page=1&dtype=recent)를 다시 확인했다.

- 최고 점수가 리더보드에 표시된다.
- Public Score는 실시간으로 최고 점수로 갱신된다.
- 각 개별 제출 점수는 제출 탭에서 확인할 수 있다.
- 동점이면 해당 점수를 먼저 기록한 팀이 높은 순위다.

따라서 마지막 제출이 이전 최고 점수를 덮어쓰지 않는다. 마감 전 최고 후보를 형식적으로 재제출할 필요도 없다. 다만 규정 위반 의심으로 점수가 삭제될 수 있으므로, 최고 점수에 해당하는 ZIP·학습 코드·설정·SHA-256을 정확히 연결해 보존한다.

### 1.2 로컬 점수와 리더보드 점수는 다른 척도다

S2가 동일 모델·동일 학습 구간으로 만든 유효한 (로컬, LB) 대응쌍이다. S1은 공식 동결 자산의 운영 앵커로 별도 보존한다.

| 모델 | 로컬 2024 점수 | 실제 LB 점수 |
| --- | ---: | ---: |
| 공식 RF **구조 재학습본** | `416.2` (2023까지 학습) | 미제출 |
| 공식 ZIP의 **동결 `rf.pkl`** | 측정 안 함 | `549.51` (운영진 실행) |
| S1 공식 ZIP의 **동결 `rf.pkl`** | 측정 안 함 | **`549.5119345223`** |
| S2 Linear 90% + HGB 10% 전체 재학습 | rolling 평균 `0.24793696` | **`527.6161010151`** |
| S3 S2 + E14 | rolling delta `-0.00033352` | **`662.3418227385`** |
| S4 S3 + E15 `r_recent3` | rolling delta `-0.00037671` | **`689.2244587204`** |
| S5 S4 + E16 | rolling delta `+0.000000339` | **`688.1692139081`** |
| S6 S4 + E22R soft probability | rolling delta `-0.000004121` | **`687.2564723096`** |
| S8 final M3 ensemble | rolling delta `-0.000005657` | **`689.3999289563`** |

두 행은 모델 자산과 학습 구간이 다르므로 `416.2 → 549.51`을 환산 관계로 사용하지 않는다.

**규칙: 로컬 점수를 LB 예상치로 환산하지 않는다.** 로컬은 후보 간 순서와 안정성을 보는 데 쓰고, 절대 수준은 LB에서 확인한다. Day 1의 공식 동결 ZIP과 우리 챔피언 제출을 서로 다른 앵커로 기록한다.

---

## 2. 제출 예산 설계

| 항목 | 수 |
| --- | ---: |
| 남은 일수 (8/17 ~ 8/31 전일 + 9/1 오전) | 15일 + α |
| 일일 한도 | 5 |
| **이론상 최대 예산** | **약 75회** |
| 실패·재시도 예비 | 10 |
| 최종 후보·운영 예비 | 10 |
| **계획된 실사용 목표** | **20~30회** |

### 2.1 일일 배분 원칙

```
슬롯 1 (오전)  전날 밤 준비된 주 후보          <- 매일 필수
슬롯 2 (오후)  당일 실험에서 나온 ablation 1건
슬롯 3 (저녁)  당일 실험에서 나온 ablation 1건
슬롯 4         예비 (실패 재시도)
슬롯 5         예비 / 명확히 다른 고가치 후보만 사용
```

### 2.2 제출 오류는 예산을 깎는다

[`COMPETITION.md` §8.5](COMPETITION.md):

- **설치 오류**(ZIP 구조, dependency 설치 실패, 설치 10분 초과) → 예산 차감 **없음**
- **제출 오류**(`script.py` 런타임 오류, 결과 미생성, 추론 10분 초과) → 예산 차감 **있음**

따라서 §7의 제출 게이트를 통과하지 않은 ZIP은 절대 올리지 않는다. 게이트 비용은 로컬 3분, 실패 비용은 하루 예산의 20%다.

### 2.3 한 번에 하나만 바꾼다

계획된 제출 수를 고려하면 **한 제출당 한 가지 변경**을 원칙으로 한다. 두 변경을 묶으면 어느 쪽이 효과인지 알 수 없고 로컬-LB 불일치의 원인도 해석하기 어렵다.

예외: 명백히 상호 의존적인 변경(예: `E14` 피처 + 그에 맞는 prior)은 한 묶음으로 본다.

---

## 3. 실험 백로그와 우선순위

세 문서의 실험 명세를 통합해 다음 순서로 확정한다. `E*`는 [야구 도메인 로드맵](research/RELATED_WORK_AND_EXPERIMENT_ROADMAP.md), `A*`는 [유사 도메인 문서](research/ANALOGOUS_DATA_METHODS_AND_EXPERIMENTS.md)의 명세다.

| # | 실험 | 근거 | 기대 | 비용 |
| ---: | --- | --- | :---: | :---: |
| **P0** | **제출 하네스 + 불변성 게이트** (`E00`/`A00`) | 미제출 = 0점 | 필수 | 중 |
| **P1 완료** | **`E14`/`A50` 시즌 내 누적 복원** | 2022~2024 rolling 3/3 개선, S3 보존 | **매우 높음** | 소 |
| **P1 완료** | **`E15`/`A52` base rate 외삽** | `r_recent3` downstream 3/3 개선, S4 보존 | **중상** | 소 |
| **P2 기각** | `E11`/`A10` EB 수축 (k는 분산 성분 고정) | 1/3 개선, 평균 Brier delta `+0.00011479` | 높음 | 소 |
| **P2 기각** | `E10` recency·regime (F 2022 이전 배제) | 2/3 개선이나 평균 `+0.00042995`, 최악 `+0.00170826` | 중상 | 중 |
| **P3 완료** | `E22R` 구종군 지도학습 mixture | soft 확률 3/3, 명시적 mixture 2/3; S6·S7 보존 | 중상 | 중 |
| **P3 보류** | `E12` CatBoost | 별도 환경 설치 불가, 후보·ZIP 미생성 | 중상 | 중 |
| **P4 기각** | `E20R` Trackman 투수 프로파일 6종 | 1/3 개선, 평균 Brier delta `+0.000001306` | 중 | 중 |
| **P4 완료** | `E16` 투수 역할·구장 | 2/3 개선, S5를 앙상블 성분으로 보존 | 중하 | 소 |
| **P5** | `A12` GA2M/EBM, `A13` leaf→Logistic, `A14` FM | Linear-HGB 사이 | 중 | 중 |
| **P6** | `E31`/`A20` 제한 앙상블 | 개별 개선 후 | 중상 | 소 |
| **P7** | `E40`/`A22` multi-fold OOF calibration | 마지막 | 중하 | 중 |

### 3.1 하지 않을 것

시간이 15일뿐이므로 다음은 명시적으로 **배제**한다.

| 배제 항목 | 이유 |
| --- | --- |
| `E30` player embedding, `A41` DeepFM/FT-Transformer | 신호 대비 variance·구현 비용 과다 |
| `E21` 다봉성 GMM, `E23` 순차 signature | plate location·release angle 부재, 제구율이 이미 빨리 안정됨 |
| `A40` FTRL/hashing | 현재 카디널리티에서 불필요 |
| 타자 중심 대규모 모델링 | 2024 split-half 단일 EB benchmark가 **38점**으로 낮아 시간 대비 기대값이 작음 |
| `pitcher × batter` raw pair 직접 EB | 같은 benchmark에서 287점으로 투수 단독(646)보다 나쁨. 단, 이 수치는 이론적 상한이 아님 |
| `A30`/`A31` 확률적 Trackman linkage | 결정적으로 이미 풀림 |
| 시퀀스·LSTM·Transformer | test에서 시퀀스 구성 불가 |

---

## 4. Phase 0 — 제출 인프라 (Day 1, 오늘)

**이것이 끝나기 전에는 다른 어떤 실험도 하지 않는다.**

### 4.1 만들 것

```text
submission/
├── build_submission.py      # 학습 -> 자산 동결 -> ZIP 패키징 한 방에
├── template/
│   ├── script.py            # 추론 진입점 (행 단위 순수 함수)
│   ├── requirements.txt
│   └── model/               # 동결 자산 출력 위치
├── verify_submission.py     # §7 게이트 전체 자동 실행
└── SUBMISSION_LOG.md        # 제출 이력 대장
```

### 4.2 `script.py` 설계 원칙

공식 `baseline_submit.zip`의 구조를 그대로 따르되 세 가지를 강제한다.

1. **행 독립 피처.** 기준 구현은 `dict` 하나(한 행)를 받아 값을 반환하는 순수 함수로 둔다. 실전 추론은 데이터프레임 벡터화를 허용하되, 다른 test 행의 값·분포·순서·개수로 집계하거나 상태를 갱신하는 연산은 금지한다. 벡터화 버전과 기준 구현이 동일한 값을 내는지 단위 테스트로 검증한다.
2. **`sample_submission.csv`의 `row_id` 순서를 기준으로 출력**한다(공식 베이스라인과 동일).
3. **모든 경로는 상대 경로**, 행 수는 어디에도 하드코딩하지 않는다.

### 4.3 방어적 처리 필수 목록

| 상황 | 처리 |
| --- | --- |
| `asof_pitcher_n == 0` | 시즌 내 통계 = prior, cold flag = 1 |
| `asof_*_rate` 결측 | 결측 indicator 보존 + prior 대치 |
| 학습에 없는 `pitcher_id`/`batter_id`/`team_id` | unknown 버킷 |
| `n_delta < 0` 또는 `s_delta < 0` 또는 `s_delta > n_delta` | `counter_invalid=1`, 시즌 내 rate는 prior fallback. 발생 건수를 stdout에 출력 |
| 예측값 `NaN`/`inf` | **즉시 실패**. clip으로 복구하려 하지 않음 |
| 유한한 예측값이 범위 밖 | 발생 건수 기록 후 `[1e-6, 1-1e-6]` clip. 원인 조사 없이는 제출 금지 |

과거 2022~2024 fold에서는 위 카운터 불변식 위반이 관측되지 않았고, 이전 시즌에 없던 투수 ID도 해당 시즌 첫 관측이 `n=0`에서 시작했다. 다만 비공개 2025 전체에 이를 가정하지 않고 세 불변식을 모두 검사한다. 독립 clamp는 `s_season > n_season` 같은 불가능한 상태를 숨기므로 사용하지 않는다.

### 4.4 Day 1 제출

| 슬롯 | 제출 | 목적 |
| --- | --- | --- |
| 1 | **S1** 공식 `baseline_submit.zip`의 동결 `rf.pkl`을 변경 없이 제출 | 공식 환경·업로드 경로 앵커. 운영진 기준은 `549.51` |
| 2 | **S2** 현재 챔피언 (Linear 90% + HGB 10%), 전체 재학습 | 우리 코드의 LB 앵커 |

S1이 `549.51`에서 크게 벗어나면 공식 ZIP의 해시·ZIP 구조·서버 로그·공식 공지 변경을 먼저 확인한다. 같은 RF 구조를 전체 데이터로 다시 학습한 모델은 **S1-R**로 별도 관리하며, S1과 같은 점수를 기대하지 않는다.

---

## 5. Phase 1 — 두 개의 큰 레버 (Day 2~4)

### 5.1 `E14` 시즌 내 누적 복원

#### 5.1.1 현재 증거 수준

- 저장된 재현 결과: 2024 단일 피처 `492.35점` (`k=50`, 과거 학습 평균 prior)
- 진단 계산: `607.26점` (`k=120`, prior `0.479`)
- **주의**: `0.479`는 2024까지 이용해 만든 2025 후보 prior이므로 607.26은 2024의 누수 없는 holdout 결과가 아니다. 우선순위 판단용 민감도 계산으로만 남긴다.
- 미확인: 2022·2023 재현, 기존 모델에 넣었을 때의 증분 이득, R/F·cold-start별 안정성

#### 5.1.2 학습·검증·추론에서 같은 정의 사용

외부 검증 시즌을 `Y`라 할 때 모든 상태와 하이퍼파라미터는 `season < Y`에서만 만든다.

```text
fold Y의 동결 상태:
    end_state_Y[p] = season < Y에서 투수 p의 마지막 투구 직후 (n_end, s_end)

검증 시즌 Y의 각 행:
    n_delta = asof_pitcher_n - n_end
    s_delta = round(asof_pitcher_success_rate × asof_pitcher_n) - s_end

prior_Y, k_Y:
    season < Y만으로 추정. Y의 Target 평균·분산·최적 점수를 사용하지 않음
```

모델 학습용 2019~2024 행도 각 행이 속한 시즌 시작 직전 상태를 빼서 같은 피처를 만든다. 한 시즌의 미래 행이나 그 시즌 전체 Target 평균을 사용하지 않는다. 최종 2025 추론에서만 2019~2024 전체로 만든 2024 종료 상태와 2024까지의 prior 추정 규칙을 사용한다.

**최종 추론 동결 자산** (train 2019~2024 전체에서 생성, `model/` 안에 저장):

```python
frozen[pitcher_id] = (n_end, s_end)
    n_end = 그 투수의 마지막 train 행 asof_pitcher_n + 1
    s_end = round(마지막 행 rate * n) + 마지막 행 control_success
```

792개 항목짜리 dict이며 파일 크기는 무시할 수준이다.

동결 사전에 없는 투수는 과거 fold에서 모두 해당 시즌 `n=0`부터 시작한 신규 ID였으므로 기본값 `(n_end=0, s_end=0)`을 사용하되 `pitcher_unseen=1`을 함께 낸다. 비공개 데이터에서도 카운터 불변식을 통과한 경우에만 시즌 내 누적으로 인정한다.

**최종 2025 행 단위 피처**:

```python
n_delta = asof_pitcher_n - n_end
s_delta = round(rate * asof_pitcher_n) - s_end
valid = (n_delta >= 0) and (0 <= s_delta <= n_delta)

if valid:
    n_season, s_season = n_delta, s_delta
    rate_season = (s_season + K * PRIOR) / (n_season + K)
else:
    n_season, s_season = 0, 0
    rate_season = PRIOR
    counter_invalid = 1
```

**만들 피처 목록**:

| 피처 | 근거 |
| --- | --- |
| `rate_season` | 저장된 2024 탐색 결과 492점. `K`는 fold 내부에서만 결정 |
| `n_season`, `log1p(n_season)` | 시즌 진행도 |
| `rate_season - rate_career` | 개인 수준 체제 변화 |
| `n_season == 0` flag | cold start |
| `pitcher_unseen` flag | 학습 cutoff 이후 처음 등장한 ID |
| `counter_invalid` flag | 비공개 데이터의 카운터 불연속 방어 |
| `reverse_season` 동일 복원 | 단독 225점 |
| ~~`middle_season`, `wide_season`, 타자 버전~~ | 2024 단독 0점 — 우선 만들지 않음 |

**필수 검사**:

- [x] 2022·2023·2024 각 fold에서 `prior_Y`, `k_Y`, 동결 상태가 검증 시즌 Target을 보지 않는가
- [x] 단일 피처와 `S2 + E14` 증분 효과가 각각 재현되는가
- [x] `n_season` 구간별 / R·F별 / 신규 투수별 Brier
- [x] 기존 `asof_*`와의 잔차 상관 — 대체인가 보완인가
- [ ] **`n_season` 포함/제외 ablation** — 시즌 진행도·선발 생존 효과가 유효한가
- [x] 불변성 테스트 (단일/배치/셔플/중복) — S3 게이트에서 차이 `0.00e+00` 확인
- [x] 실행 코드·설정·fold별 결과를 `experiments/results/` JSON으로 저장

**제출 조건**: rolling 검증 게이트를 통과한 경우에만 S3 = S2 + `E14` 피처군을 제출한다.

### 5.2 `E15` base rate 외삽

> **2026-08-17 실행 상태:** pseudo-forward 결과 `r_recent3`가 prior MAE와 standalone E14 downstream Brier 모두 최상위였다. 이를 실제 S2+E14 학습에 적용한 결과 2022·2023·2024 세 fold 모두 개선했고 평균 Brier delta는 `-0.00037671`이다. S4 ZIP은 로컬 제출 게이트를 통과해 보존했지만 최종 앙상블 전까지 업로드하지 않는다.

**후보값**:

| 방법 | 2025 예측 평균 |
| --- | ---: |
| 전체 6시즌 선형 | 0.4747 |
| 최근 3시즌 선형 | 0.4622 |
| R만 최근 3시즌 선형 | 0.4849 |
| **R 최근 3시즌 + F 단절 후 평균, train F 비중 10.9%** | **≈0.4828** |
| 2024 실측 유지 | 0.4861 |

F는 [2023 단절 때문에 선형 외삽이 무효](eda/EDA_REPORT.md)다(→ `0.2975`). 2023·2024 수준(`0.4661`)에서 출발한다. 표시된 재결합은 `0.4849 × 0.891 + 0.4661 × 0.109 = 0.4828`이다. `0.479`는 이 산식의 결과가 아니므로 후보 민감도 값으로만 다룬다.

**pseudo-forward 검증**: 2022·2023·2024를 각각 가상 미래로 두고 해당 연도 이전 정보만으로 각 규칙의 실제 오차를 측정한다. `base-rate MAE`, 최악 연도 오차, `E14`에 넣었을 때의 downstream Brier를 모두 기록한다. 검증 시즌의 실제 평균을 prior 또는 `k` 선택에 사용하지 않는다.

**적용 게이트**: 최소 2/3 fold에서 `E14` downstream Brier를 개선하고 최악 fold를 크게 악화시키지 않을 때만 2025에 적용한다. 실패하면 `E15`는 폐기하고 `E14`에는 더 보수적인 carry-forward 또는 여러 합법적 prior의 평균을 사용한다.

**적용 위치**: 한 후보 안에서는 **EB prior 또는 모델 절편 중 한 곳에만** 넣는다. 두 곳에 동시에 넣는 것은 별도 이중보정 ablation으로만 시험한다.

### 5.3 두 레버가 서로를 상쇄하지 않는지 확인 — 중요

2024 탐색 계산에서 확인한 민감도이며, 아래 두 행을 동등한 holdout 성능으로 비교하지 않는다.

| prior 평균 | 최적 K | 점수 | 예측 평균 vs 실제 |
| --- | ---: | ---: | --- |
| 학습 전체 `0.5316` (낡음) | 50 | 492 | `0.4975` vs `0.4861` (+1.14%p) |
| 2025 후보값 `0.4790`을 2024에 진단 적용 | 120 | **607** | `0.4884` vs `0.4861` (**+0.22%p**) |

이 진단은 `E14`가 현재 시즌의 리그 수준을 상당 부분 자동 반영할 가능성을 보여 준다. 다만 2024 한 fold의 결과이므로 다음처럼 보수적으로 사용한다.

- prior 오차 민감도는 `n_season` 구간별로 측정한다. 저표본 투수는 prior에 민감하고 고표본 투수는 덜 민감하다.
- 명시적 절편 보정을 **추가로** 넣으면 이중 보정 위험이 크다.
- 반드시 3조합을 비교한다: `E14`만 / 외삽만 / 둘 다.

### 5.4 `k`를 격자 탐색으로 튜닝하지 않는다

분산 성분의 탐색 근거는 `k ≈ 110~150`, 신뢰도 기반 허용 범위는 `60~200`이다. 다만 외부 fold `Y`를 평가할 때 `k_Y`는 반드시 `season < Y`만으로 정한다. 검증 시즌을 포함해 계산한 `k≈120`을 모든 과거 fold에 소급 적용하지 않는다. 후보 범위는 `60~200`으로 제한하고 fold Target 점수로 촘촘한 격자를 고르지 않는다.

---

## 6. Phase 2~4 — 모델 개선 (Day 5~12)

각 실험은 **로컬 rolling 3-fold로 먼저 거른 뒤** 통과한 것만 제출한다.

### 6.1 로컬 통과 기준 (제출 자격)

- 3-fold 평균 Brier가 현 챔피언보다 개선되거나, 잔차 상관이 낮아 앙상블 가치가 명확
- 2023·2024 fold 모두 양의 raw skill
- R/F 중 한쪽의 큰 악화를 평균으로 가리지 않음
- cold-start에서 NaN·극단 확률 없음
- 불변성 테스트 통과
- 245,789행 추론 예상 시간 < 3분 (10분 한도의 3배 여유)

### 6.2 LB 판정 기준 (챔피언 교체)

LB는 최종 목적함수지만 한 번의 차이만으로 기존 후보를 삭제하지 않는다.

1. LB가 오르면 더 높은 점수의 ZIP을 **보존 후보**로 등록한다.
2. 로컬에서 크게 나빠졌는데 LB만 오른 경우에는 즉시 유일한 챔피언으로 확정하지 않고 `LB-only` 후보로 보존한다. 2025 구조 가설을 적고 한 번의 독립 ablation으로 확인한다.
3. `5점 미만`을 임의로 동률 처리하지 않는다. 더 높은 점수의 파일도 보존하고, 표시 점수가 같거나 실행 안정성 차이가 명확할 때만 단순한 쪽을 우선한다.
4. 동일 후보의 재제출은 점수 재현성·최종 제출 규칙 확인처럼 명확한 목적이 있을 때만 한다.

### 6.3 실험별 요점

| 실험 | 핵심 |
| --- | --- |
| `E11` EB 수축 | k는 `60~200`으로 제한. prior 계층: 전역 → `game_type × hand` |
| `E10` recency | **2022 이전 F 행 배제**를 먼저 시험. 최근 4/3/2시즌, 반감기 0.5/1/2/3년 |
| `E22R` 구종군 | 1단계 `P(구종군 \| 투구 직전 피처)`를 시간 순 학습하고, 2단계 입력에는 **cross-fitted/과거 모델 확률**만 사용. hard label·현재 Trackman 값 금지 |
| `E12` CatBoost | depth 5~8, Logloss 학습 / Brier 선택. 범주 statistic에 validation target 미포함 단위 테스트 |
| `E20R` Trackman | **6개만** 사용. 학습 행은 실제 경기 날짜 이전 Trackman만 집계하고, 2025에는 2024 cutoff 자산만 사용. 주 지표는 `asof_pitcher_n` 구간별 paired Brier |
| `E16` 역할·구장 | 역할은 **offset으로만**. `home_team_id = (top_bottom=='T') ? pitcher_team_id : batter_team_id` |
| `A12` EBM | interaction 후보는 EDA 지정 6~12개부터 |

### 6.4 between/within 게이트 (`A51`)

Trackman 계열 수치 피처를 만들기 전에 반드시 `r_pooled`, `r_between`, `r_within`을 기록한다. **부호가 다르면 pooled 통계 기반 피처를 만들지 않는다.** 구속의 pooled 상관은 `+0.045`, 투수 간은 `-0.172`다.

---

## 7. 제출 게이트 — 매 제출 전 필수

`verify_submission.py` 하나로 자동 실행한다. **하나라도 실패하면 업로드하지 않는다.**

> **2026-08-18 실행 상태:** S1~S6·S8은 DACON 제출과 점수 확인을 완료했고, S7만 미제출이다. 모든 후보 ZIP·manifest·검증 보고서·SHA-256을 후보별로 동결했으며, E14/E15/E16/E22R와 최종 앙상블은 누수 없는 rolling 또는 고정 가중치 비교를 완료했다. 현재 LB 최고는 S8 `689.3999289563`, 다음은 S4 `689.2244587204`다. 상세는 [`submission/SUBMISSION_LOG.md`](submission/SUBMISSION_LOG.md), [`submission/CANDIDATE_REGISTRY.md`](submission/CANDIDATE_REGISTRY.md), [`experiments/EXPERIMENT_REGISTRY.md`](experiments/EXPERIMENT_REGISTRY.md)에 기록했다.

### 규정

- [ ] 공식 데이터 외 데이터·원격 API 미사용
- [ ] 현재 투구의 실제 위치·구종·결과·Trackman 값 미사용
- [ ] 2025 외부 데이터 미사용
- [ ] **불변성 테스트**: 단일 행 vs 배치, 순서 셔플, 중복 삽입 → 최대 절대 차이 `< 1e-12`
- [ ] test 전체 또는 다른 test 행을 이용하는 `groupby`/`rolling`/`expanding`/`fit`/분포 보정 경로가 없음 (정적 검사)
- [ ] Trackman·target encoding·prior 자산마다 `data_cutoff`가 기록되어 있음

### 파일

- [ ] ZIP 최상위에 `model/`, `script.py`, `requirements.txt` — 추가 최상위 디렉터리 없음
- [ ] `./data/test.csv`, `./data/sample_submission.csv` 상대 경로 읽기
- [ ] `./output/submission.csv` 생성
- [ ] 컬럼이 정확히 `row_id`, `control_success`, CSV index 미저장
- [ ] `row_id`가 `sample_submission`과 완전 일치, 행 수 일치
- [ ] 확률이 `[0, 1]`, `NaN`/`inf` 없음. 결측·무한대는 **즉시 실패**

### 실행

- [ ] 현재 Windows `.venv`(Python 3.11.9, 공식 핵심 패키지 버전)에서 dependency·모델 로드 성공
- [ ] `$env:PYTHONUTF8='1'`을 둔 PowerShell 모사 환경에서 `script.py` 성공
- [ ] Python 3.11.15 Ubuntu 평가 서버에서 patch 버전이나 OS에 의존하는 코드가 없도록 상대 경로·표준 라이브러리 API만 사용
- [ ] 공식 기본 환경(`pandas==2.0.3`)과 제출별 `requirements.txt`의 차이를 기록. 공식 S1 ZIP의 `pandas==2.3.3`을 모든 자체 제출에 자동 복사하지 않음
- [ ] 완전 오프라인 설치를 시험할 경우 `--no-index --find-links <wheelhouse>`로 필요한 wheel이 모두 준비됐는지 확인
- [ ] 5행 샘플과 **245,789행 모사 데이터** 양쪽에서 동작 (train에서 24.6만 행을 뽑아 test 스키마로 변환)
- [ ] 245,789행 추론 시간 < 3분, peak working set < 20GB (서버 RAM 28GB 대비 안전 여유)
- [ ] ZIP 크기 기록

### 기록

- [ ] `SUBMISSION_LOG.md`에 제출 번호, 변경점 1줄, 로컬 3-fold Brier, 예측 평균, 예상 근거, LB 점수(수신 후) 기록
- [ ] `prior_source`, `k_source`, `feature_cutoff`, 학습 코드 파일 SHA-256 기록. Git 도입 후에는 commit도 함께 기록

---

## 8. 일자별 계획

`팀 병합 마감 8/26` · `리더보드 마감 9/1 10:00`

| 일자 | 작업 | 제출 |
| --- | --- | --- |
| **D1 8/17 — 완료** | 현재 Windows 환경 고정, 제출 하네스, 불변성 게이트, 245,789행 모사 테스트 | **S1 `549.5119` / S2 `527.6161` 제출 완료** |
| **D2 8/18 — 조기 완료** | `E14` 학습·검증·추론 공통 point-in-time 구현. 2022~2024 rolling 재현 및 JSON 저장 | **S3 ZIP 준비 완료, 업로드 보류** |
| **D3 8/19 — 조기 완료** | `E15` pseudo-forward 외삽 검증. `r_recent3` prior와 `E14` downstream Brier 평가 | **S4 ZIP 준비·보존 완료, 업로드 보류** |
| **D4 8/20 — 후속 조기 완료** | `E16` 역할·구장과 `E22R` soft 확률 우선 ablation | **S5·S6 보존, 업로드 보류** |
| **D5 8/21 — 검증 완료·기각** | `E11` EB 계층 수축 | **게이트 실패, 제출 후보 없음** |
| **D6 8/22 — 검증 완료·기각** | `E10` recency — **2022 이전 F 배제** 우선 | **게이트 실패, 제출 후보 없음** |
| **D7 8/23 — 조기 완료** | `E22R` 구종군 분류기 학습 | **S6 보존, 업로드 보류** |
| **D8 8/24 — 조기 완료·부분 보류** | `E22R` 주변화 통합 / `E12` CatBoost 확인 | **S7 보존; CatBoost는 설치 불가로 보류** |
| **D9 8/25 — 보류** | `E12` CatBoost 튜닝 | **패키지 공급 복구 전 수행하지 않음** |
| **D10 8/26 — E20R 조기 검증·기각** | `E20R` Trackman 6종. **팀 병합 마감일** | **게이트 실패, 제출 후보 없음** |
| **D11 8/27 — E16 조기 완료** | `E16` 역할·구장 / `A12` EBM | **S5 보존 완료; A12는 최종 앙상블 후순위로 보류** |
| **D12 8/28 — 조기 완료** | 잔차 상관 분석 → `E31` 앙상블 후보 구성 | **S4·S5·S6·S7 성분 비교 완료** |
| **D13 8/29 — 조기 완료** | 앙상블 고정 가중 탐색 (worst-fold 확인) | **M3 선택, S8 보존** |
| **D14 8/30 — 조기 종료** | `E40` calibration — 개선 없으면 즉시 폐기 | **최종 앙상블 확정으로 추가 calibration 보류** |
| **D15 8/31 — 조기 완료** | **최종 앙상블 후보 확정.** 전체 데이터 재학습, 게이트 전체 재실행 | **S8 M3 제출·점수 확인, S7만 선택 대기** |
| **D16 9/1** | **10:00 마감. 08:00까지 최종 제출 완료** | 예비 |

### 8.1 지연 시 잘라낼 순서

계획이 밀리면 뒤에서부터 버린다: `E40` calibration → `A12` EBM → `E16` → `E20R` → `E12`. **`E14`의 누수 없는 검증은 반드시 수행**한다. 다만 `E14` 피처와 `E15` 적용 자체는 rolling 검증을 통과하지 못하면 버린다.

### 8.2 매일 저녁 루틴 (10분)

1. 그날 LB 점수를 `SUBMISSION_LOG.md`에 기록
2. 로컬 순위와 LB 순위가 어긋난 항목 표시 → 다음 실험 가설로
3. 다음 날 제출 가치가 있는 **새 후보가 있을 때만** ZIP까지 만들어 게이트 통과
4. 보존 후보들의 ZIP SHA-256, 로컬 결과, LB 점수가 정확히 연결되는지 확인

---

## 9. 리스크 레지스터

| # | 리스크 | 영향 | 대응 |
| ---: | --- | --- | --- |
| 1 | **마감까지 유효 제출 0건** | 해소 | S1·S2 서버 실행 및 점수 수신 완료 |
| 2 | 최고 점수가 어느 ZIP·코드에서 나온 것인지 추적 실패 | 큼 | §1.1의 최고 점수 규칙 확인 완료. 모든 제출의 ZIP SHA-256·설정·코드 파일 해시를 `SUBMISSION_LOG.md`에 연결 |
| 3 | test 카운터가 train에서 안 이어지거나 성공 수가 불가능 | `E14` 무력화 | `n_delta≥0`, `0≤s_delta≤n_delta` 동시 검사. 위반 행은 prior fallback + flag, 제출 전 발생 수 확인 |
| 4 | 추론 10분 초과 | 예산 차감 | 24.6만 행 모사 테스트 필수, 3분 이내 목표 |
| 5 | `pandas` 버전 불일치 | 설치 오류 | 서버 기본 2.0.3과 공식 S1 requirements 2.3.3을 구분. 제출 자산별로 필요한 버전을 고정하고 깨끗한 venv에서 재현 |
| 6 | 이중 보정으로 점수 하락 | 중 | §5.3의 3조합 비교 필수 |
| 7 | 규정 위반 판정 | 치명 | §7 불변성 게이트를 모든 제출에 강제. 순수 함수 설계 |
| 8 | 2025 F 비중이 train과 다름 | 중 | F는 약 10.9% 비중. 전역 모델 + 수축된 F residual로 노출 최소화 |
| 9 | 신규 투수 비율이 예상보다 높음 | 중 | 과거 5시즌 기준 신규 20% 내외. cold-start fallback 별도 검증 |
| 10 | 마감 직전 서버 혼잡 | 중 | D15에 최종 후보 완료. D16은 예비만 |
| 11 | 로컬 31.9GB RAM에서도 `A12`/`A13` 대형 중간 행렬의 peak 미계측 | 낮음 | 현재 파이프라인 working set은 약 1.8GB. 대형 실험은 하나씩 실행하고 peak를 별도 기록. §13.4 |
| 12 | **CatBoost 과적합 가능성** — 이전 임시 환경에서 200 iter 531.4점 → 500 iter 292.5점, 현재 PC 미재현 | 중 | 현재 PC 재현 스크립트와 시간 순 early stopping이 먼저다. §13.3 |
| 13 | **607점을 누수 없는 성능으로 오해** | 큼 | 607은 민감도 진단으로만 기록. fold별 prior/k로 만든 rolling 결과만 모델 선택에 사용 |
| 14 | **E15 prior 산식·체제 외삽 불안정** | 큼 | `0.4828` 산식 명시, pseudo-forward 적용 게이트. 실패 시 E15 폐기 |

---

## 10. 실행 금지 — 리더보드로 평가 라벨 통계를 역추정하지 않는다

이 항목은 선택 실험이나 백로그가 아니라 **금지 목록**이다.

- 같은 모델에 여러 균일 shift를 적용해 LB 점수 곡선으로 2025 base rate를 역산하지 않는다.
- 세그먼트별 예측을 조작해 R/F·선수군별 base rate를 복원하지 않는다.
- 서로 다른 모델의 `prediction_mean`과 LB 점수를 회귀해 참값 방향을 추정하지 않는다. 모델 간 discrimination 차이가 섞이므로 통계적으로도 식별되지 않는다.
- 실행 필요성이 생겨도 운영진의 명시적 서면 허용 전에는 제출하지 않는다.

로컬 2024에서는 균일 shift로 base rate를 복원할 수 있다는 수학적 진단을 확인했지만 기대 이득은 약 2점에 불과했다. [`COMPETITION.md` §9.1](COMPETITION.md)의 평가 데이터셋 유출 시도·코드 제출 기능 악용 금지에 비해 얻는 가치가 너무 작다.

`prediction_mean`은 로컬 calibration 진단과 정상 범위 확인용으로만 기록한다. LB 점수와 결합해 비공개 라벨 통계를 추정하지 않는다.

---

## 11. 실험 기록 스키마

모든 실험과 제출에 대해 다음을 남긴다(`SUBMISSION_LOG.md` + `experiments/results/*.json`).

```text
submission_id            S1, S2, ...
date / slot
change_from_previous     한 줄, 변경 1개
local_fold_brier         2022 / 2023 / 2024
local_fold_score         2022 / 2023 / 2024
local_mean_brier
worst_fold_raw_skill
segment_brier            R / F / cold-start / n_season 구간 / hand matchup
prediction_mean          <- 로컬 calibration·정상 범위 진단
prediction_std / quantiles
calibration_intercept, calibration_slope
prior_source / k_source
feature_cutoff           각 동결 통계·Trackman 자산의 마지막 허용 시점
r_pooled/between/within  Trackman 계열 피처가 있을 때 (A51)
row_independence         max abs diff (단일/셔플/중복)
predict_seconds_245789
zip_size_mb
zip_sha256
LB_score                 <- 수신 후 기입
LB_minus_local           <- 로컬-LB 괴리 추적
```

`prediction_mean`은 후보의 calibration·drift를 로컬 기준과 비교하는 진단값이다. 서로 다른 모델의 `prediction_mean`과 LB 점수 사이 관계로 2025 참값을 추정하지 않는다. `LB_minus_local` 역시 환산식이 아니라 로컬-LB 정렬이 깨진 가설을 찾는 설명용 값이다.

---

## 12. 성공 기준

| 수준 | 기준 |
| --- | --- |
| **최소** | 유효 제출 1건 이상 + Public Score `549.51` 이상 (수료 요건) |
| **목표** | 누수 없이 검증된 `E14` 적용본이 S2를 상회하고, `E15`는 적용 게이트 통과 시에만 추가 개선 |
| **상위** | Phase 2~4 누적 개선 + 안정적 앵상블로 Phase 3 진출권 |

저장된 2024 탐색에서 `E14` 단독은 492점, 현 챔피언은 409.7점이어서 개선 가능성은 확인됐다. 그러나 둘은 서로 다른 예측 파이프라인이며 `S2 + E14` 증분 효과와 rolling 재현은 아직 미측정이다. 607점은 §5.1대로 민감도 진단일 뿐 성능 기준으로 사용하지 않고, D1~D2 앵커 결과 전후 모두 로컬 점수를 LB 예상치로 환산하지 않는다.

---

## 13. 필요 자원과 실행 환경

### 13.1 결론

**현재 Windows PC에서 기존 EDA·baseline·calibration 전체를 실행할 수 있다. 당장 외부 컴퓨트는 필요 없다.**

- 기존 전체 재현 작업은 각각 5분 이내다.
- 현재 파이프라인의 실행 중 Python working set은 약 `1.7~1.8GB`로 관측됐고, RAM은 31.9GB다.
- sklearn·pandas 중심 실험은 Ryzen 5 5600 CPU로 실행한다. RTX 2070 SUPER는 기본 경로에서 사용하지 않는다.
- CatBoost 수치는 이전 임시 환경의 예비 벤치마크일 뿐 현재 PC에서 재현되지 않았다. 스크립트와 설정 파일을 만든 뒤 별도 측정하기 전에는 시간·성능 확정 근거로 쓰지 않는다.

### 13.2 로컬 사양과 측정된 실행 비용

| 항목 | 값 |
| --- | --- |
| OS·셸 | Windows 11 Pro 64-bit, Windows PowerShell 5.1 |
| CPU | AMD Ryzen 5 5600, 6코어 12스레드 |
| 메모리 | **31.9GB RAM** |
| GPU | NVIDIA GeForce RTX 2070 SUPER, 8GB VRAM |
| 디스크 | C: 여유 약 320GB (확인 시점 값) |
| Python | 로컬 3.11.9, `.venv\Scripts\python.exe` |
| 설치된 패키지 | numpy 1.26.4, pandas 2.0.3, scikit-learn 1.8.0, scipy 1.15.3, joblib 1.5.3 |
| 미설치 | catboost, lightgbm, xgboost, interpret, torch |

현재 PC 측정값(전부 실측, 2026-08-17):

| 작업 | 시간 | 메모리 상태 |
| --- | ---: | --- |
| 구조 EDA 전체 `run_structural_eda.py` | **21.8초** | 정상 완료 |
| 제1·2부 EDA 전체 재생성 | **150.1초** | 정상 완료 |
| 베이스라인 6개 모델, 2024 fold | **약 105초** | 정상 완료 |
| 베이스라인 2022~2024 세 fold | **278.2초** | 정상 완료 |
| Calibration·Ensemble 132전략 × 3 fold | **249.4초** | 실행 중 working set 약 1.7GB |

모델별 2024 fold 시간은 Linear `19.1초 + 1.2초`, RF `54.8초 + 0.6초`, HGB `24.2초 + 1.5초`다(학습 + 253,507행 예측). 전체 사양·설치·검증 기록은 [`LOCAL_ENVIRONMENT.md`](LOCAL_ENVIRONMENT.md)에 고정한다.

`E14`·`E15`·`E11`·`E10`·`E20R`·`E16`은 pandas 산술과 기존 Linear/HGB 재학습이 중심이므로 1차 예산을 **실험 하나당 2~5분**으로 잡는다. 새 고카디널리티 인코딩이나 반복 그리드를 추가하면 별도로 측정한다.

### 13.3 CatBoost 상태 — 선택적 실험 보류

2026-08-18 현재 기본 `.venv`는 변경하지 않았다. 별도 `.venv_catboost`를 만들고 `catboost==1.2.8` 설치를 시도했지만 현재 패키지 인덱스에서 호환 배포본을 찾지 못해 설치가 실패했다(`No matching distribution found`). 따라서 E12는 임의의 미검증 환경으로 강행하지 않고, 최종 앙상블 후보에는 포함하지 않았다. 아래 값은 이전 임시 환경의 예비 기록으로만 남긴다.

| 이전 환경 iterations | 이전 fit 시간 | 2024 Brier | 환산 점수 | 현재 판단 |
| ---: | ---: | ---: | ---: | --- |
| 200 | 254초 | 0.248480 | 531.4 | 재현 필요 |
| 500 | 539초 | 0.249076 | 292.5 | 과적합 신호, 재현 필요 |

이 수치는 현재 Windows PC의 성능·시간 예산으로 사용하지 않는다. 패키지 공급이 복구되어 E12를 재개할 때는 다음을 먼저 완료한다.

1. CatBoost 버전을 별도 optional requirements 또는 전용 가상환경으로 고정한다. 기본 `.venv`를 무기록 상태로 변경하지 않는다.
2. `experiments/`에 시간 순 inner split, seed, CPU/GPU 모드, thread 수를 명시한 재현 스크립트를 만든다.
3. 200·500 iteration 고정 비교보다 early stopping을 우선한다. 외부 검증 시즌은 `eval_set` 선택에 사용하지 않는다.
4. 현재 Ryzen CPU와 RTX 2070 SUPER에서 CPU/GPU 결과를 각각 측정하되, 최종 제출은 DACON L4 환경에서 같은 예측이 재현되는 경로만 채택한다.
5. 2022~2024 세 fold와 최악 fold를 본 뒤에만 CatBoost를 앙상블 후보로 승격한다.

현재까지 유효한 교훈은 **iteration 증가가 과적합을 일으킬 수 있다**는 가설뿐이다. 시간과 최적 iteration은 현재 PC 재현 후 갱신한다.

### 13.4 RAM 31.9GB 운용 원칙

기존 8GB·스왑 제약은 현재 PC에 적용되지 않는다. baseline과 calibration 실행 중 Python working set은 약 `1.7~1.8GB`로 관측됐다. 다만 이는 정식 peak 계측이 아니며, 아래 실험은 큰 중간 행렬을 만들 수 있다.

| 위험 | 예상 메모리 | 완화 |
| --- | ---: | --- |
| `A13` leaf→Logistic — HGB 250트리 × 31리프를 one-hot | 1.2M행 × 250 nonzero ≈ **2.4GB 희소행렬** | 첫 실행은 트리 50개, peak 측정 후 확대 |
| `A12` EBM — 1.47M행 × 40피처 + interaction | 현재 PC 미측정 | interaction 없이 작은 fold로 시작, peak와 시간을 기록 |

비교 가능한 시간 측정을 위해 무거운 실험은 하나씩 실행한다. 제출 모사 게이트는 peak working set `20GB` 미만을 목표로 두어 28GB 평가 서버에 안전 여유를 남긴다.

### 13.5 로컬 GPU와 외부 컴퓨트 판단

| 항목 | GPU 효과 | 비고 |
| --- | :---: | --- |
| sklearn Linear / HGB / RF | ❌ | CPU 전용. **코어 수가 전부** |
| pandas 집계, `E14`/`E15`/`E11`/`E20R` | ❌ | I/O·산술 바운드 |
| EBM | ❌ | GPU 지원 없음 |
| CatBoost / XGBoost / LightGBM | ✅ | 로컬 RTX 2070 SUPER 8GB에서 먼저 검증 가능 |
| torch FM·embedding | ✅ | §3.1에서 배제한 항목 |

현재 PC는 CPU 6코어 12스레드, RAM 31.9GB, CUDA 가능한 GPU를 모두 갖추고 있다. 따라서 외부 GPU를 기본 계획에 넣지 않는다.

외부 컴퓨트를 검토하는 조건은 세 가지다.

1. 현재 PC에서 재현 스크립트와 작은 시간 순 검증을 먼저 통과했다.
2. 로컬 VRAM·RAM·실행 시간의 실제 한계가 계측됐다.
3. 공식 데이터의 외부 플랫폼 업로드가 규정상 허용되는지 운영진의 서면 답변을 확보했다.

조건을 모두 만족하지 않으면 현재 PC에서 계속 실행한다.

### 13.6 규정 고려사항

규정은 외부 데이터와 추론 시점 원격 API를 금지하고, Phase 3 제출에는 학습 환경의 OS·라이브러리 버전을 요구한다. 동시에 [`COMPETITION.md` §9.3](COMPETITION.md)은 비참가자에게 데이터 또는 대회 자료를 전송·복제·재배포하지 못하게 한다.

따라서 Kaggle·Colab 같은 제3자 플랫폼 업로드가 private 설정만으로 충분히 허용된다고 문서에서 단정하지 않는다. 현재 PC 자원이 충분하므로 외부 플랫폼을 사용하지 않는다. 불가피해지면 데이터 업로드 전에 운영진의 명시적 서면 허용을 받는다.

### 13.7 평가 서버 자원은 여유롭다

| 항목 | 서버 | 우리 사용량 |
| --- | --- | --- |
| 추론 시간 | 10분 한도 | 단일 모델 `0.6~1.5초`; 최종 앙상블은 24.6만 행 모사로 다시 측정 |
| RAM | 28GB | 기존 실행 중 working set 약 `1.8GB`; 최종 peak 별도 계측 |
| CPU | 6 vCPU | 충분 |
| GPU | NVIDIA L4, 22.4GiB | **사용하지 않음** |
| ZIP | 10GB 한도 | Linear 32KB / HGB 427KB / RF 3.4MB; CatBoost는 현재 PC 재현 후 측정 |
| 설치 | 10분 한도 | 기본 후보는 서버 기본 패키지 사용; optional 패키지는 실제 설치 시간 측정 후 채택 |

현재 챔피언은 Linear·HGB이므로 **CPU 추론을 기본으로 고정한다.** CatBoost 등 GPU 후보가 실제로 챔피언을 이길 때만 서버 L4 경로를 별도 검증한다.

---

## 14. 이 계획을 수정하는 규칙

이 문서는 고정 계획이 아니라 **매일 갱신되는 작업 문서**다.

- LB 결과가 로컬 순위와 어긋나면 → 즉시 전체 우선순위를 뒤집지 않고 `LB-only` 후보와 설명 가설을 기록한 뒤 독립 ablation 1회로 확인한다
- 로컬 게이트를 통과한 실험이 2회 연속 LB에서 개선을 못 내면 → 백로그 하단으로 내린다
- `E14`는 누수 없는 검증 자체를 반드시 수행하되 피처 채택은 결과에 따른다. `E15`는 §5.2 적용 게이트 실패 시 폐기한다
- 제출 게이트와 금지 실험 §10은 예외 없이 유지한다
- 변경 시 §15에 한 줄로 기록한다

## 15. 계획 변경 기록

| 날짜 | 변경 |
| --- | --- |
| 2026-08-17 | EDA 제2부·두 연구 문서를 통합해 최초 작성. `E14`/`E15`를 최우선으로 배치, `A30`/`A31`·embedding·시퀀스 계열 배제 확정 |
| 2026-08-17 | §13 필요 자원 신설. 로컬 실행 비용 실측, CatBoost 벤치마크, 8GB RAM 제약과 외부 GPU 판단 기준 추가. 리스크 11·12 추가 |
| 2026-08-17 | 검토 반영: 607점을 민감도 진단으로 재분류, E14 fold별 point-in-time 명세 추가, E15 재결합 `0.4828`로 수정·적용 조건화 |
| 2026-08-17 | 공식 동결 RF와 재학습 RF 분리, LB 반복 제출 제거, 카운터·NaN 게이트 강화, base-rate 역추정 실험 금지, 예비 벤치마크 재현성 상태 명시 |
| 2026-08-17 | 현재 Windows PC 기준으로 전면 개정: PowerShell·Python 3.11.9·Ryzen 5 5600·RAM 31.9GB·RTX 2070 SUPER 반영, 재현 실측 갱신, M2/8GB 가정 제거, 최고 점수 반영 규칙 확인 완료 |
| 2026-08-17 | Phase 0 제출 파이프라인 완료. S1 공식 ZIP 원본 보존, S2 전체 재학습·결정적 ZIP 빌드, 단일행/셔플/중복 불변성과 245,789행 시간·peak RAM 게이트 통과. 실제 DACON 업로드만 대기 |
| 2026-08-17 | S1 `549.5119345223`, S2 `527.6161010151` 서버 실행 확인. S1을 LB 챔피언으로, S2를 자체 파이프라인 개선 기준점으로 확정 |
| 2026-08-17 | E14_v1 2022·2023·2024 rolling 3/3 개선, 평균 Brier delta `-0.00033352`. S3 ZIP 게이트 통과 후 후보 대장에 보존, 실제 제출은 최종 앙상블 전까지 보류 |
| 2026-08-17 | E15 pseudo-forward에서 `r_recent3` 채택. downstream 3/3 개선, 평균 Brier delta `-0.00037671`; S4 ZIP 게이트 통과 후 후보 대장에 보존 |
| 2026-08-18 | E11_v1 `game_type × pitcher_hand` EB shrinkage가 1/3 fold만 개선, 평균 Brier delta `+0.00011479`로 게이트 실패. 후보로 만들지 않고 부정 결과 보존 |
| 2026-08-18 | E10_v1 2022 이전 F 행 배제가 2/3 fold 개선했지만 평균 Brier delta `+0.00042995`, 최악 fold `+0.00170826`으로 게이트 실패. 후보로 만들지 않고 부정 결과 보존 |
| 2026-08-18 | E16_v1 선발 비율·경기 수·홈팀 문맥이 2/3 fold 개선, 평균 Brier delta `+0.000000339`, 최악 `+0.00001110`으로 형식 게이트 통과. S5를 앙상블 후보로 보존하되 단독 채택하지 않음 |
| 2026-08-18 | E22R_probs_v1 soft 구종군 확률 ablation이 3/3 fold 개선, 평균 Brier delta `-0.000004121`. S6 ZIP과 stage-1 모델을 보존하고 최종 앙상블 전 업로드 보류 |
| 2026-08-18 | E22R_mixture_v1 명시적 주변화가 2/3 fold 개선, 평균 Brier delta `-0.000003409`, 최악 `+0.000183416`. S7을 보존하되 2022 악화로 앙상블 전용 처리 |
| 2026-08-18 | E20R_v1 시즌 이전 Trackman profile이 1/3 fold만 개선, 평균 Brier delta `+0.000001306`, gate FAIL. 부정 결과 보존 후 후보 폐기 |
| 2026-08-18 | FINAL_ENSEMBLE_M3_v1 고정 가중치 비교에서 S4 70% + S5 5% + S6 10% + S7 15%가 2/3 fold 개선, 평균 Brier delta `-0.000005657`. S8 최종 앙상블 ZIP을 로컬 게이트 통과 후 보존 |
| 2026-08-18 | DACON 제출 결과 반영: S3 `662.3418227385`, S4 `689.2244587204`, S5 `688.1692139081`, S6 `687.2564723096`, S8 `689.3999289563`. S8을 현재 LB 챔피언으로 갱신하고 S7은 미제출 유지 |
