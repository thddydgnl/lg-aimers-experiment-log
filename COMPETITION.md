# LG Aimers 9기 온라인 해커톤 대회 정리

> 대회명: **Aimers 9기 : 투구 제구 성공 확률 예측 AI 온라인 해커톤**  
> 대회 ID: `236743`  
> 공식 페이지: <https://dacon.io/competitions/official/236743/overview/description>  
> 문서 확인 기준: **2026-08-17 19:55 KST**  
> 현재 Windows 개발 PC: [`LOCAL_ENVIRONMENT.md`](LOCAL_ENVIRONMENT.md)  
> 주의: 일정과 규칙은 운영 상황에 따라 바뀔 수 있다. 제출 전에는 반드시 공식 페이지와 중요 공지를 다시 확인한다.

## 1. 한눈에 보기

| 항목 | 내용 |
| --- | --- |
| 단계 | LG Aimers 9기 Phase 2 온라인 해커톤 |
| 과제 | 투구 직전까지 알 수 있는 정보로 각 투구의 제구 성공 확률 예측 |
| 문제 유형 | 이진 확률 예측, 정형 데이터 |
| Target | `control_success`: 성공 `1`, 실패 `0` |
| 제출값 | 각 행의 `control_success = 1` 확률, `0.0` 이상 `1.0` 이하 |
| 평가 지표 | Brier Skill Score 기반 점수 |
| 제출 방식 | 모델과 추론 코드를 포함한 `submit.zip`을 평가 서버에서 실행 |
| 실제 평가 데이터 | 비공개 `test.csv` 245,789행 |
| 일일 제출 | 최대 5회 |
| 팀 | 개인 또는 최대 5명, 복수 팀/개인·팀 중복 참가 불가 |
| 외부 데이터 | 사용 금지. 대회가 제공한 데이터만 사용 가능 |
| Phase 3 | 코드·PPT 제출 및 검증을 통과한 Private 상위권 약 100명 선발 |

가장 중요한 마감은 대회 종료가 아니라 **리더보드 제출 마감인 2026-09-01 10:00 KST**다. 이 시각 이후에는 새 리더보드 제출을 할 수 없다.

## 2. 문제 정의

각 행은 투구 한 건의 **투구 직전 상태**다. 모델은 다음과 같은 사전 정보만으로 그 투구가 제구에 성공할 확률을 출력해야 한다.

- 볼·스트라이크·아웃 카운트
- 이닝, 초/말, 점수 차, 주자 상태, 상황 중요도
- 익명화된 투수·타자·팀 정보
- 현재 투구 이전까지 계산된 `asof_*` 과거 이력
- 대회가 제공한 2019~2024년 Trackman 과거 로그에서 합법적으로 만든 피처

온라인 해커톤에서 다음 세 유형은 제구 실패로 정의된다.

1. 스트라이크존 가운데 부근으로 들어간 공
2. 스트라이크존에서 크게 벗어난 공
3. 포수의 요구 방향과 반대로 들어간 공

그 외 유효한 투구가 제구 성공이다. 학습 데이터의 `control_success`는 이 정의에 따른 이진 정답이고, 제출 시에는 0/1 분류값이 아니라 **성공 확률**을 내야 한다.

현재 투구의 실제 위치, 판정, 결과, 실제 구종, Trackman 측정값처럼 투구 이후에 확정되는 정보는 입력에 사용할 수 없다.

## 3. 대회의 목적과 참가 자격

- Phase 2는 온라인에서 문제 해결 능력을 평가하여 Phase 3 오프라인 해커톤 진출자를 선발하는 과정이다.
- LG Aimers 9기 교육생이라면 Phase 1 이수 여부와 관계없이 Phase 2에 참가할 수 있다.
- 다만 Phase 3에는 **Phase 1과 Phase 2를 모두 이수한 교육생**만 참가할 수 있다. Phase 1 미이수자는 Phase 2 성적이 좋아도 Phase 3에 진출할 수 없다.
- 참가 신청 시 성명과 LG Aimers 가입 이메일을 입력한다. DACON 계정 이메일과 LG Aimers 이메일은 같지 않아도 된다.
- 공식 Q&A에 따르면 Phase 2에서 구성한 팀은 Phase 3에서도 그대로 유지된다.

주최는 LG AI연구원, 주관은 DACON, 참여사는 한경닷컴이다.

## 4. 일정

모든 시각은 KST(UTC+9) 기준이다.

| 구분 | 일정 | 비고 |
| --- | --- | --- |
| 대회 시작 | 2026-08-05 10:00 | Phase 2 시작 |
| 팀 병합 마감 | 2026-08-26 23:59 | 이후 팀 구성/병합 불가 |
| **리더보드 제출 마감** | **2026-09-01 10:00** | 대회 종료 하루 전, 실질적인 최종 제출 마감 |
| 대회 종료 | 2026-09-02 10:00 | 페이지 헤더에는 09:59까지로도 표시됨 |
| 코드·PPT 제출 | 2026-09-02 12:00 ~ 2026-09-07 10:00 | Phase 3 진출 희망 상위권 대상 |
| 코드 검증 | 2026-09-07 10:00 ~ 2026-09-11 |  |
| Phase 3 진출자 발표 | 2026-09-14 10:00 |  |
| Phase 3 예정 | 2026-09-19 ~ 2026-09-20 | 1박 2일 오프라인, 변동 가능 |

마감 직전 서버 혼잡이나 10분 추론 제한 초과를 고려해 최종 후보 제출은 충분히 일찍 완료하는 편이 안전하다.

## 5. 제공 데이터

### 5.1 파일 구조

현재 저장소의 배포 데이터는 다음 구조다.

```text
open/
├── baseline_submit.zip
├── data/
│   ├── train.csv
│   ├── test.csv
│   ├── sample_submission.csv
│   └── trackman_history.csv
└── data_description.md
```

| 파일 | 역할 | 크기/형태 |
| --- | --- | --- |
| `train.csv` | 학습 입력과 정답 | 1,475,092행 × 49열 |
| `test.csv` | 로컬 형식 검사용 평가 샘플 | 5행 × 48열 |
| `sample_submission.csv` | 로컬 형식 검사용 제출 샘플 | 5행 × 2열 |
| `trackman_history.csv` | 추가 피처용 Trackman 과거 로그 | 1,793,078행 × 30열 |
| `baseline_submit.zip` | 서버 제출 구조와 경로를 확인할 수 있는 공식 베이스라인 | `model/`, `script.py`, `requirements.txt` 포함 |

실제 평가 서버에서는 샘플 `test.csv`와 `sample_submission.csv`가 각각 **245,789행**짜리 비공개 파일로 교체된다. 따라서 코드에 5행이라는 가정을 넣으면 안 된다.

로컬 파일을 확인한 결과:

- `train.csv` 시즌 범위: 2019~2024
- `train.csv`의 `control_success` 평균: 약 `0.523766`
- 배포 `test.csv` 샘플의 시즌: 2025
- `trackman_history.csv` 시즌 범위: 2019~2024

전체 컬럼 정의와 사용 금지 정보는 [`open/data_description.md`](open/data_description.md)를 참고한다.

### 5.2 메인 데이터의 컬럼군

- 식별자·경기 정보: `row_id`, `season`, `game_month`, `game_dayofweek`, `inning`, `top_bottom`, `game_type`
- 카운트·점수: `balls_before`, `strikes_before`, `outs_before`, 각종 득점과 점수 차
- 주자·상황: 각 베이스 주자 여부, `base_state`, 기대 승률, `li`
- 선수·팀: 익명 투수/타자 ID, 좌우 유형, 팀 ID
- 과거 이력: 투수·타자 누적 표본 수와 성공률, 최근 경기 통계, 구종군 사용 비율 등 `asof_*`
- 정답: 학습 데이터에만 있는 `control_success`

표본 수가 0인 cold-start 행에서는 일부 비율 피처가 결측일 수 있다. 결측 대치, smoothing, 선수/리그 평균 fallback을 모델 파이프라인 안에서 일관되게 처리해야 한다.

### 5.3 Trackman 데이터 사용 범위

`trackman_history.csv`는 메인 데이터와 행 단위로 바로 붙이는 정답 테이블이 아니라, 과거 투구 특성이나 투수 단위 통계를 만들기 위한 보조 로그다.

공식 Q&A에서 운영진은 다음 활용을 허용한다고 답변했다.

- 학습 데이터와 Trackman 로그를 비교해 `pitcher_id`와 `pitcher_trackman_id`의 대응을 추정
- 그 대응을 사용해 해당 시점 이전의 평균 구속·무브먼트 등 투수 단위 요약 피처 생성

단, 학습 행을 만들 때는 해당 투구 시점 이후의 Trackman 기록이 섞이지 않도록 해야 한다. 현재 투구의 Trackman 측정값과 제공되지 않은 2025년 Trackman 데이터는 사용할 수 없다.

## 6. 가장 중요한 규칙: 평가 행 독립 예측

운영진은 2026-08-13 중요 공지에서 이 원칙 위반으로 실격 사례가 계속 발생하고 있다고 재안내했다.

> 특정 행의 예측은 그 행의 입력, 그 행에서 만든 파생변수, 공식 학습 데이터에서 학습한 고정된 통계·모델만으로 결정되어야 한다.

### 허용되는 예

- 학습 데이터만으로 fit한 전처리기와 모델을 평가 각 행에 적용
- 한 행 내부의 카운트, 점수, 주자 상태를 조합한 피처
- 학습 데이터만으로 만든 선수별 prior, smoothing 통계, target encoding
- 운영진이 제공한 `asof_*` 피처
- 각 행의 예측이 서로 영향을 주지 않는 단순 배치 추론

### 금지되는 예

- `test.csv`의 다른 행을 이용한 선수·팀·월·경기별 집계
- test 내부 rolling, lag, expanding, 누적 통계
- test 전체의 평균, 표준편차, 빈도, 순위, 분포를 이용한 전처리나 보정
- test 내부 범주 빈도, target encoding, 시점 정렬 후 과거처럼 보이는 행 활용
- 평가 배치 구성이나 행 순서에 따라 한 행의 예측이 달라지는 로직

### 제출 전 필수 불변성 테스트

임의의 평가 행 `A`에 대해 아래 두 결과가 같아야 한다.

1. `A` 한 행만 모델에 넣어 얻은 확률
2. `A`를 다른 여러 행과 함께 넣어 얻은 `A`의 확률

또한 test 행 순서를 섞거나 중복 행을 추가해도 원래 행의 확률이 부동소수점 허용 오차 안에서 동일해야 한다. 이 검사는 모든 제출 후보에 대한 실격 방지 게이트로 두는 것이 좋다.

생성형 AI로 코드를 작성하는 것은 허용되지만, 생성된 코드가 위 원칙을 지키는지 확인할 책임은 참가자에게 있다. 위반 제출은 나중에 삭제를 요청해도 검토 대상에서 제외되지 않는다.

## 7. 평가 방식

### 7.1 공식 산식

평가 지표는 Brier Skill Score를 100,000배한 형태다.

```text
Brier Score = mean((p_i - y_i)^2)
r = mean(y_i)
기준 Brier Score = r × (1 - r)

Score = max(0, 100000 × (1 - Brier Score / 기준 Brier Score))
```

- `p_i`: i번째 행의 제구 성공 예측 확률
- `y_i`: 실제 정답 0 또는 1
- `r`: 비공개 평가 데이터 전체의 평균 제구 성공률

해석:

- 완벽한 예측의 이론상 점수는 `100000`이다.
- 평가 데이터의 평균 성공률만 내는 climatology 기준은 Brier Skill Score `0`, 즉 점수 `0`에 해당한다.
- 기준보다 나쁜 모델도 `max(0, ...)` 때문에 점수는 0으로 잘린다.
- `549.51`점은 Brier Skill Score로 `0.0054951`, 즉 평균 성공률 기준 Brier Score보다 약 0.55% 개선된 수준이다.

이 지표는 단순 정분류 여부가 아니라 **확률값의 정확성과 calibration**을 평가한다. 따라서 validation에서도 공식 산식 또는 최소한 Brier Score를 주 지표로 사용해야 하며, 0/1 accuracy 최적화만으로는 대회 목표를 제대로 반영하지 못한다.

### 7.2 Public/Private와 선발

현재 평가 탭의 설명은 다음과 같다.

- Public Score: 전체 테스트 데이터 100%
- Private Score: 대회 종료 시점의 Public Score
- 공식 DAKER FAQ는 **최고 점수가 리더보드에 표시되고 Public Score가 실시간 최고 점수로 갱신된다**고 안내한다.
- 1차 평가는 Private Score 100%로 순위를 정한다.
- 동점이면 해당 점수를 먼저 기록한 팀이 우선한다.
- Phase 3 진출 희망 상위권은 코드와 PPT를 제출하고 코드 검증을 통과해야 한다.
- 최종적으로 검증을 통과한 Private 상위권 약 100명 규모가 Phase 3에 진출한다.

현재 설명대로라면 일반적인 Public/Private 데이터 분할은 없고, 대회 종료 시점까지 기록된 **최고 Public 점수**를 Private 결과로 확정하는 구조다. 마지막 제출이 기존 최고 점수를 덮어쓰지는 않는다. 다만 규정 위반 의심으로 점수가 삭제될 수 있으므로 최고 점수에 해당하는 ZIP·코드·SHA-256은 별도로 보존해야 한다.

### 7.3 LG Aimers 수료 기준

공식 평가 탭에 기재된 수료 조건은 다음 두 가지다.

1. Phase 1 이수
2. Phase 2 Public Score `549.51` 이상

`549.51`은 운영진 베이스라인 추론 코드를 공식 평가 환경에서 실행한 점수다. 이는 Phase 3 진출 순위 기준과 별개의 **최소 수료 점수 기준**이다.

## 8. 코드 제출 형식

### 8.1 제출 ZIP

대회별 안내에 맞춰 다음 구조로 `submit.zip`을 만든다.

```text
submit.zip
├── model/
│   └── ...               # 모델 가중치와 추론에 필요한 로컬 자산
├── script.py             # 평가 서버가 실행하는 추론 코드
└── requirements.txt      # pip install -r 형식
```

ZIP 안에 `submit/` 같은 추가 최상위 폴더를 넣으면 안 된다. `model/`, `script.py`, `requirements.txt`가 압축을 풀었을 때 바로 최상위에 있어야 한다.

평가 서버는 실행 시 다음 항목을 추가한다.

```text
working-directory/
├── model/
├── script.py
├── requirements.txt
├── data/                 # 읽기 전용 실제 평가 데이터
│   ├── test.csv
│   └── sample_submission.csv
└── output/
    └── submission.csv    # script.py가 반드시 생성
```

이 저장소에 포함된 공식 `baseline_submit.zip`도 `./data/test.csv`, `./data/sample_submission.csv`, `./model/...`, `./output/submission.csv` 상대 경로를 사용한다.

### 8.2 결과 파일 요구사항

- 경로와 파일명: `output/submission.csv`
- 컬럼 순서: `row_id`, `control_success`
- 행 수: 실제 `test.csv`와 동일한 245,789행
- `row_id`: 평가 데이터와 정확히 일치
- `control_success`: 성공 확률인 0~1 실수
- CSV index를 별도 열로 저장하지 않는다.

### 8.3 용량과 시간 제한

| 항목 | 제한 |
| --- | --- |
| ZIP 크기 | 최대 10GB |
| 압축 해제 후 크기 | 최대 32GB |
| 패키지 설치 | 최대 10분 |
| 전체 추론 | 최대 10분, 245,789행 |

### 8.4 평가 서버

이 절은 **현재 Windows PC가 아니라 DACON의 원격 Ubuntu 평가 서버** 사양이다. 로컬 사양과 실행 명령은 [`LOCAL_ENVIRONMENT.md`](LOCAL_ENVIRONMENT.md)를 따른다.

| 항목 | 환경 |
| --- | --- |
| OS | Ubuntu 22.04.5 LTS |
| Python | 3.11.15 |
| CPU | 6 vCPU |
| RAM | 28GB |
| GPU | NVIDIA L4, VRAM 22.4GiB |
| CUDA | 12.8 |
| 네트워크 | 패키지 설치 외 인터넷 비활성화 |

공식 평가 탭의 주요 기본 패키지:

```text
torch==2.7.1+cu128
pandas==2.0.3
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0
narwhals==2.21.2
transformers==4.46.3
accelerate==1.9.0
sentencepiece==0.1.99
regex==2023.12.25
tqdm==4.66.4
loguru==0.7.2
pyyaml==6.0.1
rich==13.7.1
```

페이지는 기본 설치된 패키지는 가능하면 `requirements.txt`에 중복 기재하지 말 것을 권장한다. 온라인 다운로드가 필요한 `from_pretrained(...)`, 외부 API, 원격 DB 등은 추론 중 작동하지 않으므로 모델·토크나이저·설정 파일을 모두 ZIP의 `model/` 아래에 포함해야 한다.

### 8.5 오류와 제출 횟수

- **설치 오류**: ZIP 구조 불일치, dependency 설치 실패, 설치 제한시간 초과. 일일 제출 횟수가 차감되지 않는다.
- **제출 오류**: `script.py` 실행 중 오류, 결과 파일 미생성, 추론 제한시간 초과 등. 일일 제출 횟수가 차감된다.

모든 경로는 작업 디렉터리 기준 상대 경로로 쓰고, 샘플 5행과 실제 245,789행 양쪽에서 동일한 코드가 동작하도록 한다.

## 9. 일반 규칙

### 9.1 모델·도구·데이터

- 사전학습 모델은 가중치가 누구에게나 공개되어 있고, 최소한 비상업적 이용을 허용하는 라이선스가 있어야 한다.
- OpenAI API, Gemini API처럼 원격 서버를 호출해야만 쓸 수 있는 모델/API는 금지된다.
- 공식 제공 데이터 외의 외부 데이터는 금지된다.
- 적법한 라이선스를 보유하고 다른 규칙을 지키면 AutoML 도구 사용은 허용된다.
- 사용 언어는 Python이다.
- 평가 데이터셋 유출 시도나 코드 제출 기능 악용은 실격 사유다.

### 9.2 개인·팀

- 개인 또는 최대 5명 팀으로 참가할 수 있다.
- 한 사람은 하나의 DACON 계정과 하나의 개인/팀 참가 상태만 가져야 한다.
- 팀 구성 시 모든 팀원에게 최소 1회 제출 기록이 필요하다.
- 병합 가능 여부를 계산할 때 팀원들의 누적 제출 횟수를 합산한다. 개인으로 사용한 제출 기회가 팀 병합 후 새로 생기지 않는다.
- 팀 구성 직후 팀원 기존 제출 중 최고 점수가 팀에 공유된다.
- 아이디어 유출 방지를 위해 팀 탈퇴 후 재구성은 불가능하다고 공식 Q&A에 안내되어 있다.

### 9.3 데이터와 코드 공유

- 대회 데이터는 이 대회 참가와 제출물 개발에만 사용할 수 있다.
- 비참가자에게 데이터 또는 대회 자료를 전송·복제·재배포하면 안 된다.
- 일반 규정상 대회 관련 코드를 공개 공유할 때는 DACON 플랫폼을 이용해야 한다.
- 제출물은 제3자의 지식재산권과 라이선스를 침해하지 않아야 한다.

## 10. Phase 3용 코드·PPT 제출

Phase 3 진출을 희망하는 Private 상위권 팀은 종료 후 지정 기간에 `dacon@dacon.io`로 다음을 제출한다.

- 학습 코드 개발 환경의 OS와 라이브러리 버전
- Private Score를 재현하는 학습 코드 (`.py` 또는 `.ipynb`, UTF-8)
- 자유 형식 솔루션 PPT
- 각 팀원의 Phase 3 오프라인 참가 여부

추론 코드는 이미 리더보드에 제출한 ZIP의 코드로 대체된다. 코드와 PPT 제출만으로 진출이 보장되는 것은 아니며, 재현·규칙 검증까지 통과해야 한다.

## 11. 상금과 수상자 조건

Phase 2 자체에는 상금이 없다. Phase 3 상위 3팀에 총 1,000만 원이 수여된다.

| 순위 | 상금 | 상 |
| --- | ---: | --- |
| 1위 | 500만 원 | 고용노동부장관상 |
| 2위 | 300만 원 | LG AI연구원장상 |
| 3위 | 200만 원 | 한경닷컴 사장상 |

제세공과금은 개인 부담이다.

동의사항에서 특히 확인할 내용:

- 수상자는 결과 발표일로부터 5년간 스폰서에게 제출물과 관련 소스 코드에 대한 전 세계적·비독점적·완전 지불된 이용 권한을 부여한다.
- 허용 목적은 스폰서의 내부 연구, 기술 개발, 제품·서비스 시험 및 파일럿으로 제한된다.
- 기간을 넘기거나 상업 판매, 제3자 이용 허락·양도를 하려면 수상자와 별도 협의 및 합당한 대가가 필요하다고 규정한다.
- 청년일경험 지원사업과 중복되는 다른 정부 취업지원·훈련사업 참여 여부, 취업·고용보험 상태 등 개인 자격 확인 조항이 있다. 해당 가능성이 있으면 참가 전에 운영기관에 확인한다.

## 12. 실험 설계에 주는 함의

아래는 공식 규칙을 바탕으로 한 이 저장소의 실무적 권장사항이며, 대회 운영진의 추가 규칙은 아니다.

1. **시간 순 validation을 우선한다.** 학습 데이터가 2019~2024이고 평가 샘플이 2025이므로, 예를 들어 2019~2023으로 학습하고 2024로 검증하는 방식이 무작위 분할보다 실제 평가 상황에 가깝다.
2. **Brier Score를 직접 최적화한다.** 분류 accuracy보다 확률 calibration, smoothing, ensemble 평균, out-of-fold calibration의 효과를 검증한다.
3. **모든 통계 피처는 cutoff를 가진다.** 학습 행에서도 해당 투구 이후 기록을 사용하지 않고, Trackman 집계 역시 시점 기준을 명시한다.
4. **cold start를 별도로 본다.** `asof_*_n == 0`, 신규 선수, 희소 선수 구간의 Brier Score를 따로 기록한다.
5. **test 독립성 테스트를 자동화한다.** 단일 행/배치, 순서 셔플, 중복 삽입 결과가 같은지 CI 또는 제출 전 스크립트에서 검사한다.
6. **리더보드 점수뿐 아니라 실행 예산도 추적한다.** 모델 크기, peak RAM/VRAM, 245,789행 추론 예상 시간, ZIP 크기를 실험 메타데이터에 남긴다.
7. **제출 ZIP을 오프라인으로 재현한다.** 빈 환경에서 dependency 설치와 `python script.py` 실행, 결과 스키마 검증까지 한 번에 확인한다.

## 13. 제출 전 체크리스트

### 규칙

- [ ] 공식 데이터 외의 데이터나 원격 API를 사용하지 않았다.
- [ ] 사전학습 모델/가중치의 공개 범위와 라이선스를 기록했다.
- [ ] 현재 투구 이후의 정보가 어떤 피처에도 들어가지 않았다.
- [ ] test 다른 행의 값·분포·순서가 한 행의 예측에 영향을 주지 않는다.
- [ ] 단일 행/배치 불변성 테스트를 통과했다.

### 파일

- [ ] ZIP 최상위에 `model/`, `script.py`, `requirements.txt`가 있다.
- [ ] 추가 최상위 디렉터리가 없다.
- [ ] `data/test.csv`와 `data/sample_submission.csv`를 상대 경로로 읽는다.
- [ ] `output/submission.csv`를 생성한다.
- [ ] 결과 컬럼이 정확히 `row_id`, `control_success`다.
- [ ] 행 수, ID 일치, 결측·무한대, 확률 범위 `[0, 1]`를 검사한다.

### 실행

- [ ] 인터넷이 없는 환경에서도 모델 로드와 추론이 가능하다.
- [ ] Python 3.11과 공식 패키지 버전에서 테스트했다.
- [ ] 설치 10분, 추론 10분, RAM 28GB, VRAM 22.4GiB 제한을 만족한다.
- [ ] 압축 10GB, 해제 후 32GB 제한을 만족한다.
- [ ] 샘플 5행에 하드코딩된 로직이 없다.
- [ ] 최종 후보를 2026-09-01 10:00보다 충분히 일찍 제출했다.

## 14. 현재 공식 자료 사이의 주의할 차이

1. 대회 헤더는 종료를 `2026-09-02 09:59`까지로 표시하고, 일정 탭은 종료 시각을 `10:00`으로 표시한다. 10:00을 경계 시각으로 보고 그보다 일찍 처리한다.
2. 평가 탭의 서버 구조, 범용 코드 제출 가이드, 동봉 베이스라인은 모두 `data/` 경로를 사용하지만, 평가 탭 하단 유의사항 한 문장에는 `open/` 데이터를 로드하라고 적혀 있다. 현재 실행 기준은 `data/`로 보는 것이 일관되지만, 최종 제출 전 최신 공식 공지를 다시 확인한다.
3. 범용 코드 제출 가이드는 ZIP 파일명을 자유롭게 정할 수 있다고 설명하지만, 이 대회별 페이지는 `submit.zip`으로 안내한다. 대회별 안내에 맞춰 `submit.zip`을 쓰는 편이 안전하다.
4. 서버 기본 패키지 표는 `pandas==2.0.3`을 안내하지만 동봉 베이스라인 `requirements.txt`는 `pandas==2.3.3` 설치를 요청한다. 제출 환경을 추측하지 말고 실제 ZIP 기준으로 의존성을 고정하고 로컬에서 재현한다.

모호한 사항은 토크 게시글 댓글 또는 `dacon@dacon.io`로 문의하고, 답변 링크와 확인 날짜를 이 문서에 추가한다.

## 15. 공식 자료 링크

- [대회 개요](https://dacon.io/competitions/official/236743/overview/description)
- [데이터](https://dacon.io/competitions/official/236743/data)
- [평가 및 코드 제출 환경](https://dacon.io/competitions/official/236743/overview/evaluation)
- [규칙](https://dacon.io/competitions/official/236743/overview/rules)
- [일정](https://dacon.io/competitions/official/236743/overview/schedule)
- [상금](https://dacon.io/competitions/official/236743/overview/prize)
- [동의사항](https://dacon.io/competitions/official/236743/overview/agreement)
- [DACON 코드 제출 대회 가이드](https://cfiles.dacon.co.kr/competitions/236564/guide.html)
- [평가 데이터 독립 예측 원칙 위반 사례 재안내](https://dacon.io/competitions/official/236743/talkboard/417123)
- [리더보드 제출 코드 규칙 리마인드](https://dacon.io/competitions/official/236743/talkboard/417094)
- [팀 병합 및 제출 횟수 기준](https://dacon.io/competitions/official/236743/talkboard/417093)
- [대회 공식 FAQ/Q&A](https://dacon.io/competitions/official/236743/talkboard/417082)
- [로컬 상세 데이터 설명서](open/data_description.md)

## 16. 문서 갱신 기록

| 날짜 | 내용 |
| --- | --- |
| 2026-08-16 | 공식 개요·데이터·평가·규칙·일정·상금·동의사항, 코드 제출 가이드, 중요 공지와 Q&A를 기준으로 최초 작성 |
