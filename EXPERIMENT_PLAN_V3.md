# 통합 실험 계획서 v3/v4 — 예상 1,190점 지속 개선 GOAL

> **V5 사후 정정(2026-08-21):** `V4_compact_supported_1193`의 실제 LB는 사용자 보고
> 약 `1005`로, 이 문서의 예상 `1193.0915`가 전이되지 않았다. 아래 V4 완료 판정은
> 역사 기록이며 현재 Goal 기준이 아니다. 활성 계획은 [`EXPERIMENT_PLAN_V5.md`](EXPERIMENT_PLAN_V5.md)다.

> 시작: 2026-08-20 KST  
> 현재 실제 LB: `1090.9100565103` (`V3_sparse_m3_1103`, 제출 ID `57386`)  
> 목표: **사전 고정한 누수 방지 환산식의 예상 LB가 1,190점을 초과할 때까지 반복 개선**  
> 실행 주체: Codex GOAL. 실제 DACON 업로드는 사람이 한다.

> **1,100 단계 완료: 2026-08-20 KST.** 최우선 후보 `V3_sparse_m3_1103`은 2024
> 로컬 `963.5501`, 고정 환산 예상 LB `1103.6977`(`1101.5328~1108.9599`)이다.
> 실제 제출 결과는 `1090.9100565103`으로 예상보다 `12.7876`점 낮았다. 예상점수
> 완료 조건은 충족했지만 실제 LB는 1,100을 넘지 못했다.

## 0. 최종 결과

| 후보 | 구성 | 2022 | 2023 기록용 | 2024 | 고정 예상 LB | 판단 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `V3_sparse_m3_1103` | A `0.501444` + C `0.270160` + B `0.228396` | `2445.2773` | `0` | `963.5501` | `1103.6977` | **1순위**, S8 보수 앵커도 1,100 초과 |
| `V3_sparse_m2_1100` | A `0.629362` + B `0.370638` | `2440.2546` | `0` | `960.5052` | `1100.6527` | **2순위**, 중앙 예상값만 1,100 초과 |

### 실제 DACON 제출 결과 — 2026-08-20

| 순위 | 후보 | 제출 ID | 제출 시각 | 예상 LB | 실제 LB | 예상 오차 |
| ---: | --- | ---: | --- | ---: | ---: | ---: |
| 1 | `V3_sparse_m3_1103.zip` | `57386` | `21:16:03` | `1103.6976957` | **`1090.9100565103`** | `-12.7876392` |
| 2 | `V3_sparse_m2_1100.zip` | `57388` | `21:16:21` | `1100.6527444` | `1088.5196116458` | `-12.1331327` |
| 3 | `V_ensemble.zip` | `57391` | `21:16:47` | `836.6776` | `906.8719072396` | `+70.1943` |
| 4 | `V_base.zip` | `57394` | `21:18:49` | `822.0025` | `879.8414124135` | `+57.8389` |
| 5 | `S11.zip` | `57395` | `21:19:11` | 미산정 | `879.8414124135` | 미산정 |

후보 순위는 M3 → M2 → V2 ensemble → base 순으로 맞았지만, 기존 S4·S5·S6·S8
오프셋은 새 모델 family 전체에 동일하게 적용되지 않았다. M3·M2에서는 약 12점
과대추정했고 V2 tree family에서는 58~70점 과소추정했다. 따라서 이후 예상식은
family별 실제 제출 앵커가 없으면 절대점수 예측보다 후보 순위 판단에만 사용한다.

A는 rich Trackman 전체 프로파일+투수 outcome component, B는 과거 월·카운트·손·이닝률,
C는 component를 뺀 rich Trackman 모델이다. 세 모델은 모두 공식 과거 데이터만 쓰는
`reverse_any` 보조 outcome CatBoost이고, 최종 보정은 사전 비교한 작은 격자의
`clip(0.5 + 1.05 × (p - 0.5) - 0.006)`이다. test 집계값은 사용하지 않는다.

M3의 V2 ensemble 대비 pitcher-cluster paired Brier delta는 2024
`-0.00066703`, 95% CI `[-0.00080442, -0.00053854]`; 2022는
`-0.00030152`, 95% CI `[-0.00043805, -0.00016772]`로 안전 게이트를 통과했다.
2023은 공식 F 라벨 체제 단절 때문에 계획대로 기록만 한다. 상세 재현값은
[`experiments/results/v3_sparse_ensemble.json`](experiments/results/v3_sparse_ensemble.json)에 있다.

## 1. 공식 규칙 경계

2026-08-20에 DACON의 개요·규칙·평가 탭을 로그인 상태에서 다시 확인했다.

- `open/`의 공식 제공 데이터만 사용한다. 외부 데이터는 사용하지 않는다.
- OpenAI/Gemini 등 외부 API와 원격 추론은 사용하지 않는다.
- 평가 행은 서로 독립이어야 한다. test의 다른 행, 전체 분포, 빈도, 순서, 배치 통계를
  이용한 보정은 만들지 않는다.
- 각 행의 입력값과 train에서 동결한 모델·조회표만 사용한다.
- 추론 10분, 설치 10분, ZIP 10GB(압축 해제 32GB), 6 vCPU·RAM 28GB 한도를 지킨다.
- 일일 제출은 5회다. 파이프라인은 ZIP까지만 만들며 자동 제출하지 않는다.

## 2. 예상 점수의 사전 고정 정의

모델을 본 뒤 유리한 환산식을 고르지 않기 위해, 기존에 실제 제출한 네 후보의
`실제 LB - 같은 2024 fold 로컬 점수`를 시작 전에 고정한다.

| 앵커 | 실제 LB | 2024 로컬 | 오프셋 |
| --- | ---: | ---: | ---: |
| S4 | `689.2244587204` | `547.2021142017` | `142.0223445187` |
| S5 | `688.1692139081` | `542.7594531415` | `145.4097607666` |
| S6 | `687.2564723096` | `548.9836499451` | `138.2728223645` |
| S8 | `689.3999289563` | `551.4172760528` | `137.9826529035` |

```text
anchor_i(candidate) = candidate의 2024 공식산식 로컬점수 + offset_i
예상 LB             = 네 anchor_i의 중앙값
예상 범위           = min(anchor_i) ~ max(anchor_i)
```

중앙 오프셋은 `140.1475834416`이므로 **예상 LB > 1,100**에 필요한 2024 로컬점수는
`> 959.8524165584`다. 가장 보수적인 S8 앵커까지 1,100을 넘기는 참고선은
`> 962.0173470965`다. 이 값은 실제 LB 보장이 아니라, 기존 제출 결과에 고정한
의사결정용 기대값이다.

V2의 `V_ensemble`은 로컬 `696.5299753441`, 예상 LB `836.68`
(`834.51~841.94`)로 출발점이다.

## 3. V2 감사에서 발견한 우선 결함

1. `b3_hgb`가 최강 `base+e14 HGB`가 아니라 성능이 낮은 `pitcher_te` 분기를 튜닝했다.
2. LightGBM/CatBoost의 2024 rolling fold는 체제가 단절된 2023 전체를 내부
   early-stopping으로 사용했다. 각각 8회·2회에서 멈춰 예측이 과소학습됐다.
3. 더 심각하게, rolling은 위 iteration 수를 썼지만 제출 빌드는 기본값
   1200회·1500회로 학습했다. V2 부스터의 로컬 검증과 ZIP 모델이 같은 recipe가 아니다.
4. 최근 시즌 창, 시즌 감쇠 가중치, R/F 체제별 학습 범위가 최강 모델에서 비교되지 않았다.
5. 2024는 반복 탐색에 이미 사용했으므로 bootstrap CI를 확증적 증거로 과장하지 않는다.
6. 과거 LightGBM 실패는 `lightgbm 4.5.0`과 `scikit-learn 1.8.0`의 API 불일치였다.
   `4.7.0`으로 올리고 Windows DLL import 순서를 고정했으며, 프로젝트 래퍼의 일반 fit과
   시간순 early-stopping smoke test를 preflight에 추가했다. 최종 V3 ZIP은 LightGBM에
   의존하지 않고 `catboost==1.2.8`만 설치한다.

## 4. 반복 실행 순서

### P0 — 계약·재현성 고정

- 공식 규칙 스냅샷, 점수 환산식, 기존 ZIP SHA를 보존한다.
- V2 결과를 덮어쓰지 않고 모든 V3 ID를 별도 저장한다.
- booster의 iteration 선택을 rolling과 최종 빌드에서 동일하게 만든다.
- 내부 validation은 `마지막 history season의 R 행`을 사용하고, F 체제 단절의 영향을
  별도로 기록한다. 고정 iteration 대조군도 둔다.

### P1 — 가장 큰 미실험 영역

1. 최강 `base+e14 HGB`를 직접 leaf·learning-rate·L2·iteration 튜닝한다.
2. 수정된 LightGBM/CatBoost를 다시 측정한다.
3. 각 모델의 예측 평균·표준편차·선택 iteration을 필수 로그로 남긴다.

### P2 — 시간 drift와 표본 선택

- 전체 history / 최근 3시즌 / 최근 2시즌을 비교한다.
- R은 최근 시즌 감쇠, F는 2023 체제 단절 이후만 쓰는 regime-aware arm을 비교한다.
- state/E14 동결 자산은 전체 과거로 만들고, 모델 fit 행만 창·가중치로 제한한다.
- 2023 fold는 기록만 하고 2024 주 지표와 2022 보조 지표로 판단한다.

### P3 — 도메인 신호

- 여러 shrinkage 강도의 시즌 내 투수율과 표본량 상호작용.
- 시즌 평균을 제거한 직전 시즌 투수 효과와 투수×타자 손 residual.
- 기존 E22R 구종군 확률, Trackman 투수 프로파일은 저표본 투수 구간에서만 재검토한다.
- 타자 raw ID, pitcher×batter raw pair, count_state는 기존 상한 실험에 따라 제외한다.

### P4 — 보정과 앙상블

- 보정 파라미터는 이전 fold에서만 학습해 다음 fold에 적용한다.
- 단순 평균 이동, Platt/affine, R/F별 intercept를 비교하되 test 분포는 절대 읽지 않는다.
- 같은 OOF 행의 잔차 상관을 이용해 family당 1~3개만 남기고 음이 아닌 simplex 가중치를 쓴다.
- 2024만 보고 선택한 가중치와 2022까지 포함한 강건 가중치를 모두 기록한다.

### P5 — 패키징

- 상위 후보는 개별 ZIP·build JSON·verification JSON·SHA-256으로 동결한다.
- rolling과 최종 학습의 feature, fit window, sample weight, iteration 선택을 manifest에서 대조한다.
- 단일행·셔플·중복·배치 불변성, 245,789행, 시간·RAM·구조 게이트를 전부 통과한다.

## 5. 한 실험의 채택 기준

- 주 지표: 2024 공식 Brier Skill Score.
- 보조 지표: 2022 Brier와 주요 세그먼트(R/F, 시즌 내 n, cold start).
- 2023은 체제 단절 진단용이며 선택 점수에 합산하지 않는다.
- 동일한 가설에서 설정을 많이 비교할수록 winner's curse를 명시한다.
- 개선 후보는 V2 최강 예측 대비 pitcher-cluster paired bootstrap point가 음수여야 한다.
- ZIP 후보 승격 전에는 2024 CI 상한 `< 0`, 2022 Brier 악화 `<= 0.0005`를 요구한다.

## 6. GOAL 완료 조건

다음이 모두 참일 때만 완료한다.

- [x] 2024 로컬점수 `963.5501123 > 959.8524166`, 예상 LB 중앙값 `1103.6976957 > 1,100`
- [x] 점수 산식과 S4·S5·S6·S8 앵커를 시작값 그대로 유지
- [x] rolling과 최종 ZIP의 feature·outcome scheme·500 iteration·seed·보정 recipe 일치
- [x] 2024 paired bootstrap 및 2022 보조 안전 게이트 통과
- [x] 공식 규칙의 행 독립성·외부 데이터/API 금지 준수
- [x] 245,789행 모사, 10분·28GB, ZIP 구조, SHA 게이트 통과
- [x] 모든 긍정·부정 실험, OOF 예측, M2·M3 ZIP 및 대장 별도 보존

최종 ZIP은 [`submission/dist/V3_sparse_m3_1103.zip`](submission/dist/V3_sparse_m3_1103.zip)과
[`submission/dist/V3_sparse_m2_1100.zip`](submission/dist/V3_sparse_m2_1100.zip)이며 둘 다
2026-08-20 제출을 완료했다. GPU
CatBoost 재학습은 98,340행 probe에서 최대 `1.045e-8`의 수치 차이가 있었으므로 바이트
재빌드 동일성을 주장하지 않는다. 대신 최종 ZIP을 SHA-256으로 동결했고, 같은 ZIP의
반복 추론 및 행 독립성은 오차 `0`으로 검증했다.

임계값에 못 미치면 “파이프라인이 끝났다”는 이유만으로 GOAL을 완료하지 않는다. 다음
잔차 진단에서 정보량이 남은 합법적 축을 선택해 P1~P4를 반복한다.

## 7. 2026-08-21 V4 예상 1,190 확장 결과

V3 실제 LB가 예상보다 `12.7876`점 낮았기 때문에 같은 GOAL의 목표만 `1,190`으로
높여 실험을 재개했다. 시작 전에 고정한 환산식은 바꾸지 않았다.

```text
예상 LB = 2024 로컬 점수 + 140.1475834416
1,190 통과에 필요한 2024 로컬 점수 = 1049.8524165584
```

### 7.1 딥러닝 실험

공식 train만 사용하고, 모든 category encoder와 상태량은 대상 시즌 이전 데이터로만
동결했다. CUDA에서 MLP, DeepFM, TabTransformer, TabM, RealMLP, residual network를
각각 binary/outcome 손실과 여러 seed·용량으로 비교했다. 대표 2024 단독 결과는 다음과
같다. 점수 `0`은 DACON 식에서 음수 raw skill이 0으로 잘린 경우다.

| 계열 | 대표 설정 | 2024 로컬 점수 | 판단 |
| --- | --- | ---: | --- |
| embedding MLP | binary / outcome | `485.2713` / `525.0661` | 단독 후보 제외 |
| DeepFM | binary / outcome | `621.2335` / `652.9980` | 단독 후보 제외 |
| RealMLP | binary / outcome | `689.1723` / `0` | binary만 다양성 신호 |
| TabTransformer | binary / outcome / R-CE | `243.7205` / `516.8649` / `628.0207` | 단독 후보 제외 |
| TabM | binary / outcome / outcome CE k32 | `680.1480` / `820.4164` / **`916.9032`** | 딥러닝 단독 최고 |
| neural residual | 시점 고정 residual | `992.0939` | 작은 보조 개선 |

딥러닝 OOF 성분만 조합한 사전 고정 확인 중 최고는 `1008.4905`였고, TabM을 포함한
7-arm supported meta stack은 2024 `1052.0165`, 고정 예상 `1192.1640`으로 목표선을
처음 넘었다. 다만 이 조합은 signed coefficient와 별도 GPU 모델 런타임을 함께 배포해야
했다. 이어서 동일한 신호 방향을 공식 데이터 기반 CatBoost arm들로 압축한 조합이 더
높은 점수와 간단한 CPU 재현성을 보여 최종 패키지로 선택됐다. 즉 딥러닝 실험을 생략한
것이 아니라, 실제 비교 결과 최종 ZIP의 직접 성분에서는 제외한 것이다.

### 7.2 최종 선택

최종 후보는 teacher-residual student에 18개 arm의 V3 anchor 대비 편차를 더한다.

```text
p_final = clip(p_student + Σ c_i × (p_arm_i - p_v3_anchor), 1e-6, 1 - 1e-6)
```

| 후보 | 2022 | 2023 기록용 | 2024 | 고정 예상 LB | 판단 |
| --- | ---: | ---: | ---: | ---: | --- |
| V3 anchor | `2445.2773` | `0` | `963.5501` | `1103.6977` | 실제 LB 챔피언 기준선 |
| deep 포함 supported meta | `2406.7308` | 미사용 | `1052.0165` | `1192.1640` | 목표 통과, 배포 복잡도 큼 |
| `V4_compact_supported_1193` | `2413.7661` | `0` | **`1052.9440`** | **`1193.0915`** | **최종 미제출 1순위** |

V4의 V3 대비 Brier delta는 2024 `-0.0002233120`, 2022 `+0.0000785145`,
2023 `+0.0004236633`이다. 이전 시즌 허용 악화 한도 `0.0005` 안에 모두 들어와 안전
게이트를 통과했다. 2023은 기존 계획처럼 체제 단절 기록용일 뿐 선택 점수에는 넣지 않았다.

고정식 예상값 `1193.0915`는 GOAL 판정용이며 실제 LB 보장값이 아니다. V3 M3에서 관측한
과대추정 `12.7876`점이 그대로 반복된다는 민감도 가정에서는 약 `1180.30`이다. 모델
family와 평가 분포가 달라 이 보정값도 실제 점수 예측으로 확정하지 않는다.

### 7.3 전체 재학습·패키징 검사

- 공식 2019~2024 train `1,475,092`행으로 22개 모델을 전체 재학습했다.
- 구성은 V3 anchor 3개 + teacher-residual student 1개 + 최종 arm 18개다.
- 최종 ZIP: `submission/dist/V4_compact_supported_1193.zip`
- 크기: `26,677,480` bytes, 57개 멤버, 압축 해제 `68,142,539` bytes
- SHA-256: `49708fe3e6a6b4f472e0771ba396f5eacb69ecf5a38d91406fc0049b628754b0`
- research와 ZIP sample 최종 예측 최대 차이: `2.220446049250313e-16`
- 셔플·중복·단일행 불변성 최대 차이: `0`
- 245,789행 모사: `37.60초`, peak `1.50 GiB` — 10분/28GB 통과
- 동일 입력 재빌드 SHA-256 일치, ZIP root/requirements/static scan 통과
- 외부 데이터·API·test 집계·다른 test 행·test 순서 사용 없음

근거 파일은
[`experiments/results/v4_compact_supported_ensemble.json`](experiments/results/v4_compact_supported_ensemble.json),
[`submission/records/V4_compact_supported_1193_build.json`](submission/records/V4_compact_supported_1193_build.json),
[`submission/dist/V4_compact_supported_1193.verification.json`](submission/dist/V4_compact_supported_1193.verification.json)에
동결했다. DACON 업로드는 자동 수행하지 않았으며 실제 LB는 사용자가 제출한 뒤 별도로
기록한다.

### 7.4 확장 GOAL 완료 조건

- [x] 2024 로컬 `1052.9439576 > 1049.8524166`
- [x] 고정 예상 `1193.0915411 > 1190`
- [x] 딥러닝 5개 계열 이상과 residual/OOF 조합을 실제 시간순 검증
- [x] 2022·2023 이전 시즌 안전 진단 통과
- [x] 전체 학습과 ZIP runtime의 성분·최종 예측 parity 통과
- [x] 행 독립성, 245,789행, 10분/28GB, ZIP 구조·SHA·결정적 재빌드 통과
- [x] 중간 결과·OOF·모델·최종 ZIP·빌드/검증 기록 개별 보존
