"""Kaggle offload helpers.

The package is deliberately NOT named `kaggle`: a local directory with that name
would shadow the installed `kaggle` PyPI package and break the CLI this module
depends on.
"""
